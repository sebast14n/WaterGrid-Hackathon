services:
  redis:
    image: redis:7
    command: >
      redis-server
      --appendonly yes
      --requirepass STRONG_PASSWORD
    ports:
      - "6379:6379"
    volumes:
      - ./redis/data:/data
